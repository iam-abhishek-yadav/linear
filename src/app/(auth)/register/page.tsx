import { WorkspaceRegisterForm } from "@/components/auth/workspace-register-form";

export default function RegisterPage() {
  return (
    <WorkspaceRegisterForm description="Start a new organization. If you received an invite link, open that instead." />
  );
}
